obstacle factor:
- What is measurement, seems to always be 0
- what is position (to center of obstacle, to nearest point?)
- Can we get an obstacle id, so we can look up in the environment for more details?

tracking factor:
- Is path_deviation squared distance or what units?

Variables:
- It seems like the factorgraph id is the same as entity id or agent id or robot id. This could be correct, but lets confirm it.
- 

api functions:
- simular to the request_step and is_step_requested, we want a extract